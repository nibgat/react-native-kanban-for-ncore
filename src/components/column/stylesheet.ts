import {
    StyleSheet
} from "react-native";

const styles = StyleSheet.create({
    container:{
        overflow: "visible"
    },
    contentContainer:{
        overflow: "visible"
    },
    titleContainer:{
        justifyContent: "center",
        alignItems: "center"
    },
    title:{
        textAlign: "center"
    }
});
export default styles;
